# 작업지시서: P5T1

## 📋 기본 정보

- **작업 ID**: P5T1
- **업무명**: Unit Tests
- **Phase**: Phase 5
- **Area**: Testing (T)
- **서브 에이전트**: test-engineer
- **작업 방식**: AI-Only

---

## 🎯 작업 목표

컴포넌트 + API 유틸 + 함수 유닛 테스트 (Jest)

---

## 🔧 사용 도구

```
[Claude 도구]
Read, Edit, Write, Grep, Glob, Bash

[기술 스택]
Jest, Playwright, Testing Library

[전문 스킬]
test-runner, test-coverage
```

**도구 설명**:
- **Claude 도구**: Claude Code의 기본 기능 (Read, Write, Edit, Bash, Glob, Grep 등)
- **기술 스택**: 프로젝트에 사용되는 프레임워크 및 라이브러리
- **전문 스킬**: Anthropic 빌트인 스킬 (.claude/skills/*.md 참조)

## 🔗 의존성 정보

**의존성 체인**: P1F1, P3BA1, P3BA2, P3BA3

이 작업을 시작하기 전에 다음 작업이 완료되어야 합니다: P1F1, P3BA1, P3BA2, P3BA3

---

## 📦 기대 결과물

**생성할 파일:**
1. `jest.config.js` - Jest 설정
2. `__tests__/components/**/*.test.tsx` - 컴포넌트 테스트
3. `__tests__/utils/**/*.test.ts` - 유틸 함수 테스트
4. `__tests__/api/**/*.test.ts` - API 함수 테스트
5. `__tests__/setup.ts` - 테스트 환경 설정

**설치 필요 패키지:**
```bash
npm install -D jest @testing-library/react @testing-library/jest-dom @testing-library/user-event jest-environment-jsdom
npm install -D @types/jest ts-jest
```

**package.json 스크립트:**
```json
{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  }
}
```

**테스트 커버리지 목표:** 80% 이상

**주요 테스트 대상:**
- 컴포넌트: Button, Card, Form, Modal 등
- 유틸: profanity-filter, image-upload, file-upload
- API: Supabase client, middleware

**구현해야 할 세부 항목**:

1. Jest 설정 (jest.config.js, TypeScript 지원)
2. 컴포넌트 단위 테스트 (React Testing Library)
3. API 유틸 함수 테스트 (Supabase mocking)
4. Helper 함수 테스트 (profanity-filter, image-upload)
5. Mock 데이터 설정 (__mocks__ 폴더)
6. 테스트 커버리지 80% 이상 달성

각 항목을 체계적으로 구현하고 테스트하세요.

---

## 💾 구현 파일 저장 위치

**루트 폴더**: `6_Test/`

**파일 경로**:
```
6_Test/
└── __tests__/
    ├── components/
    │   └── **/*.test.tsx
    ├── utils/
    │   └── **/*.test.ts
    └── api/
        └── **/*.test.ts
```

**절대 경로 별칭**: `@/` (예: `import ... from '@/components/...'`)

---

## 📝 작업 지시사항

### 1. 준비 단계

- 프로젝트 루트 디렉토리에서 작업 시작
- 필요한 도구 확인: Jest/Playwright/Testing Library
- 의존성 작업 완료 확인 (P1F1) (P3BA1) (P3BA2) (P3BA3)

### 2. 구현 단계

**구현해야 할 세부 항목**:

1. Jest 설정
2. 컴포넌트 단위 테스트 (React Testing Library)
3. API 유틸 함수 테스트
4. Helper 함수 테스트
5. Mock 데이터 설정
6. 테스트 커버리지 80% 이상

각 항목을 체계적으로 구현하고 테스트하세요.

### 3. 검증 단계

- 작성한 코드의 정상 동작 확인
- 타입 체크 및 린트 통과
- 필요한 경우 단위 테스트 작성
- 코드 리뷰 준비

### 4. 완료 단계

- 생성된 파일 목록 확인
- PROJECT GRID 상태 업데이트
- 다음 의존 작업에 영향 확인

---

## ✅ 완료 기준

- [ ] Unit Tests 기능이 정상적으로 구현됨
- [ ] 기대 결과물이 모두 생성됨
- [ ] 코드가 정상적으로 빌드/실행됨
- [ ] 타입 체크 및 린트 통과
- [ ] PROJECT GRID 상태 업데이트 완료
- [ ] 모든 테스트가 통과함
- [ ] 테스트 커버리지 80% 이상

---

**작업지시서 생성일**: 2025-11-06
**PROJECT GRID Version**: v4.0
