# 정치인 글쓰기 데이터베이스 스키마 수정 완료 보고서

**작성일:** 2025-12-02
**작업자:** Claude Code
**작업 상태:** ✅ 마이그레이션 파일 생성 완료

---

## 📋 작업 개요

오세훈 명의로 정치인 글쓰기/댓글쓰기 테스트를 진행한 결과, 데이터베이스 스키마와 API 코드 간 불일치를 발견하여 수정 작업을 완료했습니다.

---

## 🔍 발견된 문제

### 1. `politician_sessions` 테이블 미생성
- **문제**: 마이그레이션 파일(`053_create_politician_sessions.sql`)은 존재하지만 데이터베이스에 적용되지 않음
- **영향**: 정치인 세션 토큰 관리 불가능
- **상태**: ⚠️ 마이그레이션 적용 필요

### 2. `posts` 테이블 스키마 불일치
- **문제**: `user_id` 컬럼이 `NOT NULL` 제약조건으로 설정되어 있음
- **영향**: 정치인 게시글 작성 시 `user_id`를 NULL로 설정할 수 없어 실패
- **에러 메시지**:
  ```
  null value in column "user_id" of relation "posts" violates not-null constraint
  ```

### 3. `author_type` 컬럼 미존재
- **문제**: API 코드에서는 `author_type='politician'` 사용하지만 실제 테이블에 해당 컬럼 없음
- **영향**: 일반 사용자와 정치인을 구분할 수 없음
- **에러 메시지**:
  ```
  Could not find the 'author_type' column of 'posts' in the schema cache
  ```

### 4. 테이블 스키마 확인 결과

**현재 `posts` 테이블 컬럼:**
```
id, user_id, title, content, category, tags, is_pinned, is_locked,
view_count, comment_count, share_count, moderation_status,
moderation_reason, moderated_at, moderated_by, created_at, updated_at,
upvotes, downvotes, politician_id
```

**부족한 부분:**
- ❌ `author_type` 컬럼 없음
- ❌ `user_id` NULL 허용 안 됨

---

## 🔧 수행한 작업

### 1. Migration 053 확인
- **파일**: `053_create_politician_sessions.sql`
- **위치**: `0-4_Database/Supabase/migrations/`
- **상태**: ✅ 파일 존재, 적용 대기
- **내용**:
  - `politician_sessions` 테이블 생성
  - 세션 토큰 관리 (64자리 hex)
  - 만료 시간 관리 (영구 세션: 2099-12-31)
  - IP 주소 및 User-Agent 저장
  - 자동 정리 함수

### 2. Migration 054 생성
- **파일**: `054_fix_politician_posting_schema.sql`
- **위치**: `0-4_Database/Supabase/migrations/`
- **상태**: ✅ 신규 생성 완료

**수정 내용:**

#### A. `posts` 테이블 수정
```sql
-- 1. user_id NULL 허용
ALTER TABLE posts
  ALTER COLUMN user_id DROP NOT NULL;

-- 2. author_type 컬럼 추가
ALTER TABLE posts
  ADD COLUMN author_type TEXT NOT NULL DEFAULT 'user'
  CHECK (author_type IN ('user', 'politician'));

-- 3. CHECK 제약조건 추가
ALTER TABLE posts
  ADD CONSTRAINT posts_author_check
  CHECK (
    (user_id IS NOT NULL AND politician_id IS NULL AND author_type = 'user') OR
    (user_id IS NULL AND politician_id IS NOT NULL AND author_type = 'politician')
  );

-- 4. 기존 데이터 업데이트
UPDATE posts SET author_type = 'user' WHERE author_type IS NULL;
```

#### B. `comments` 테이블 수정 (동일한 패턴)
```sql
-- posts와 동일한 수정 사항 적용
ALTER TABLE comments ALTER COLUMN user_id DROP NOT NULL;
ALTER TABLE comments ADD COLUMN author_type TEXT NOT NULL DEFAULT 'user';
ALTER TABLE comments ADD CONSTRAINT comments_author_check CHECK (...);
UPDATE comments SET author_type = 'user' WHERE author_type IS NULL;
```

#### C. 인덱스 추가 (성능 최적화)
```sql
-- 정치인 게시글/댓글 조회용
CREATE INDEX idx_posts_politician_id ON posts(politician_id);
CREATE INDEX idx_comments_politician_id ON comments(politician_id);

-- author_type별 조회용
CREATE INDEX idx_posts_author_type ON posts(author_type);
CREATE INDEX idx_comments_author_type ON comments(author_type);
```

### 3. 마이그레이션 적용 가이드 작성
- **파일**: `MIGRATION_APPLY_GUIDE.md`
- **위치**: `0-4_Database/Supabase/migrations/`
- **내용**:
  - Supabase Dashboard를 통한 적용 방법
  - Supabase CLI를 통한 적용 방법
  - psql 직접 연결 방법
  - 적용 후 검증 방법
  - 롤백 방법
  - 문제 해결 가이드

---

## 📊 변경 사항 요약

### Before (현재 상태)
```sql
CREATE TABLE posts (
  id UUID PRIMARY KEY,
  user_id UUID NOT NULL,  -- ❌ NULL 불가
  politician_id TEXT,
  title TEXT,
  content TEXT,
  -- author_type 컬럼 없음 ❌
  ...
);
```

### After (마이그레이션 적용 후)
```sql
CREATE TABLE posts (
  id UUID PRIMARY KEY,
  user_id UUID,  -- ✅ NULL 허용
  politician_id TEXT,
  title TEXT,
  content TEXT,
  author_type TEXT NOT NULL DEFAULT 'user',  -- ✅ 추가
  CONSTRAINT posts_author_check CHECK (
    (user_id IS NOT NULL AND politician_id IS NULL AND author_type = 'user') OR
    (user_id IS NULL AND politician_id IS NOT NULL AND author_type = 'politician')
  ),
  ...
);
```

---

## ✅ 마이그레이션 적용 방법

### 🚀 가장 간단한 방법: Supabase Dashboard

1. **Supabase Dashboard 접속**
   - https://supabase.com/dashboard
   - 프로젝트: PoliticianFinder

2. **SQL Editor 열기**
   - 왼쪽 사이드바 → SQL Editor → New query

3. **Migration 053 실행**
   ```sql
   -- 053_create_politician_sessions.sql 내용 복사 붙여넣기
   -- Run 버튼 클릭
   ```

4. **Migration 054 실행**
   ```sql
   -- 054_fix_politician_posting_schema.sql 내용 복사 붙여넣기
   -- Run 버튼 클릭
   ```

5. **검증**
   ```sql
   -- 1. politician_sessions 테이블 확인
   SELECT * FROM politician_sessions LIMIT 1;

   -- 2. posts 테이블 author_type 컬럼 확인
   SELECT column_name, data_type, is_nullable
   FROM information_schema.columns
   WHERE table_name = 'posts'
     AND column_name IN ('user_id', 'author_type', 'politician_id');
   ```

**자세한 방법은 `MIGRATION_APPLY_GUIDE.md` 참조**

---

## 🧪 마이그레이션 적용 후 테스트 방법

### 1. 데이터베이스 직접 테스트
```sql
-- 정치인 게시글 삽입
INSERT INTO posts (user_id, politician_id, title, content, category, author_type)
VALUES (NULL, '62e7b453', '[테스트] 오세훈 게시글', '테스트 내용', '정책발표', 'politician')
RETURNING *;

-- 정치인 댓글 삽입
INSERT INTO comments (post_id, user_id, politician_id, content, author_type)
VALUES ('[POST_ID]', NULL, '62e7b453', '오세훈 댓글', 'politician')
RETURNING *;
```

### 2. Node.js 테스트 스크립트 실행
```bash
cd 1_Frontend
node test_politician_posting_simple.js
```

**예상 결과:**
```
========================================
정치인 글쓰기/댓글쓰기 간단 테스트
========================================

Step 1: 정치인 정보 확인
✅ 정치인 정보 확인 성공
   이름: 오세훈
   소속: 국민의힘
   직책: 서울특별시장

Step 2: 게시글 작성
✅ 게시글 작성 성공
   Post ID: [UUID]
   제목: [테스트] 서울시 청년 정책 추진 현황

Step 3: 댓글 작성
✅ 댓글 작성 성공
   Comment ID: [UUID]

========================================
✅ 모든 테스트 통과!
========================================
```

---

## 📝 생성/수정된 파일

### 생성된 파일

1. **`0-4_Database/Supabase/migrations/054_fix_politician_posting_schema.sql`**
   - posts/comments 테이블 스키마 수정
   - author_type 컬럼 추가
   - CHECK 제약조건 추가

2. **`0-4_Database/Supabase/migrations/MIGRATION_APPLY_GUIDE.md`**
   - 마이그레이션 적용 상세 가이드
   - 검증 방법
   - 롤백 방법
   - 문제 해결

3. **`1_Frontend/test_politician_posting_simple.js`**
   - 정치인 글쓰기/댓글쓰기 간단 테스트 스크립트
   - 실제 DB 스키마에 맞춰 작성

### 기존 파일 (참조)

1. **`0-4_Database/Supabase/migrations/053_create_politician_sessions.sql`**
   - 이미 존재하는 마이그레이션 파일
   - 적용 필요

---

## 🎯 다음 단계

### 필수 작업 (마이그레이션 적용 후)

1. **✅ 데이터베이스 타입 재생성**
   ```bash
   cd 1_Frontend
   supabase gen types typescript --project-id yqihnwqljtrddxhvmpkz > src/lib/database.types.ts
   ```

2. **✅ API 코드 확인**
   - `src/app/api/posts/route.ts`
   - `src/app/api/comments/route.ts`
   - `src/lib/auth/politicianSession.ts`

   이미 `author_type='politician'` 사용하도록 구현되어 있음 ✅

3. **✅ 테스트 실행**
   ```bash
   node test_politician_posting_simple.js
   ```

4. **✅ DATABASE_SCHEMA.md 업데이트**
   - posts 테이블 스키마 업데이트
   - comments 테이블 스키마 업데이트
   - politician_sessions 테이블 추가

### 선택 작업

1. **E2E 테스트 작성**
   - 정치인 인증 전체 플로우
   - 이메일 인증 → 세션 생성 → 글쓰기

2. **프론트엔드 UI 구현**
   - 정치인 전용 글쓰기 페이지
   - 정치인 프로필 표시
   - 정치인 게시글 배지

---

## ⚠️ 주의사항

### 1. 마이그레이션 적용 순서
반드시 **순서대로** 적용해야 합니다:
1. Migration 053 (politician_sessions 테이블 생성)
2. Migration 054 (posts/comments 스키마 수정)

### 2. 기존 데이터 영향
- ✅ **기존 게시글**: 자동으로 `author_type='user'`로 설정됨
- ✅ **기존 댓글**: 자동으로 `author_type='user'`로 설정됨
- ✅ **데이터 손실 없음**: 모든 기존 데이터 보존

### 3. 롤백 가능
- 마이그레이션 적용 후 문제 발생 시 롤백 가능
- 롤백 스크립트는 `MIGRATION_APPLY_GUIDE.md` 참조

---

## 📊 테스트 결과 예상

### ✅ 성공 케이스

**1. 정치인 게시글 작성**
```json
{
  "id": "uuid",
  "user_id": null,
  "politician_id": "62e7b453",
  "title": "[테스트] 서울시 청년 정책 추진 현황",
  "content": "...",
  "author_type": "politician",
  "created_at": "2025-12-02T..."
}
```

**2. 정치인 정보와 함께 조회**
```sql
SELECT p.*, pol.name, pol.party, pol.position
FROM posts p
JOIN politicians pol ON p.politician_id = pol.id
WHERE p.author_type = 'politician';
```

**결과:**
```
title: [테스트] 서울시 청년 정책 추진 현황
name: 오세훈
party: 국민의힘
position: 서울특별시장
```

### ❌ 에러 케이스 (제약조건 위반)

**1. user_id와 politician_id 둘 다 있는 경우**
```sql
INSERT INTO posts (user_id, politician_id, title, content, author_type)
VALUES ('user-uuid', '62e7b453', '제목', '내용', 'politician');
-- ❌ ERROR: violates check constraint "posts_author_check"
```

**2. user_id와 politician_id 둘 다 없는 경우**
```sql
INSERT INTO posts (user_id, politician_id, title, content, author_type)
VALUES (NULL, NULL, '제목', '내용', 'user');
-- ❌ ERROR: violates check constraint "posts_author_check"
```

**3. author_type 불일치**
```sql
INSERT INTO posts (user_id, politician_id, title, content, author_type)
VALUES (NULL, '62e7b453', '제목', '내용', 'user');
-- ❌ ERROR: violates check constraint "posts_author_check"
```

---

## 📚 관련 문서

### 마이그레이션 파일
- `0-4_Database/Supabase/migrations/053_create_politician_sessions.sql`
- `0-4_Database/Supabase/migrations/054_fix_politician_posting_schema.sql`

### 가이드 문서
- `0-4_Database/Supabase/migrations/MIGRATION_APPLY_GUIDE.md`

### 기존 보고서
- `Web_ClaudeCode_Bridge/outbox/politician_id_타입_일관성_수정_완료_보고서.md`
- `Web_ClaudeCode_Bridge/outbox/코드_품질_개선_완료_보고서.md`
- `1_Frontend/정치인_글쓰기_구현_완료_보고서.md`

### 테스트 스크립트
- `1_Frontend/test_politician_posting_simple.js`

---

## ✅ 결론

### 완료된 작업
- ✅ 문제 진단 및 원인 파악
- ✅ Migration 053 확인 (`politician_sessions` 테이블)
- ✅ Migration 054 생성 (posts/comments 스키마 수정)
- ✅ 마이그레이션 적용 가이드 작성
- ✅ 테스트 스크립트 작성

### 남은 작업
- ⚠️ **사용자가 직접 수행 필요**: Supabase에 마이그레이션 적용
  - Migration 053 적용
  - Migration 054 적용
  - 검증 실행

### 마이그레이션 적용 후
- ✅ 정치인 글쓰기/댓글쓰기 기능 완전 작동
- ✅ 오세훈 명의로 게시글/댓글 작성 가능
- ✅ 일반 사용자와 정치인 명확히 구분
- ✅ 데이터 무결성 보장 (CHECK 제약조건)

---

**작성자:** Claude Code
**최종 업데이트:** 2025-12-02

**📌 다음 작업:** Supabase Dashboard에서 마이그레이션 053, 054를 순서대로 적용하고 테스트 실행
