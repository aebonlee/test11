# V15.0 자동화 아키텍처 설계서

**작성일**: 2025-11-14
**버전**: V15.0
**담당**: AI 평가 엔진 개발팀

**V15.0 주요 변경사항**:
- Rating 범위: -8~+8 → **-6~+10** (17단계, 비대칭)
- 점수 범위: 200~1,000 → **300~1,000** (1,000점 상한 적용)
- 데이터 출처 비율: **OFFICIAL 60% + PUBLIC 40%** 규칙 추가

---

## 1. 시스템 개요

### 1.1 목적

관리자 대시보드에서 버튼 클릭 한 번으로 정치인 평가를 완전 자동화

### 1.2 작동 흐름

```
[관리자 대시보드]
      ↓ 버튼 클릭
[Backend API]
      ↓ Python 스크립트 실행
[main_orchestrator.py]
      ↓ 병렬 실행
[data_collector_parallel.py] → 10개 카테고리 동시 수집
      ↓ 데이터 저장
[Supabase DB]
      ↓ 점수 계산
[score_calculator.py]
      ↓ 결과 업데이트
[DB + Frontend]
      ↓ 실시간 표시
[관리자 대시보드]
```

---

## 2. 시스템 아키텍처

### 2.1 컴포넌트 구성

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (Next.js)                   │
│  ┌─────────────────────────────────────────────────┐   │
│  │         관리자 대시보드                           │   │
│  │  [정치인 평가 시작] 버튼                          │   │
│  │  진행 상황 실시간 표시                            │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────┬───────────────────────────────────┘
                      │ HTTP POST
                      ↓
┌─────────────────────────────────────────────────────────┐
│                 Backend API (Next.js)                   │
│  ┌─────────────────────────────────────────────────┐   │
│  │  POST /api/evaluate-politician                  │   │
│  │  - politician_id, politician_name 수신          │   │
│  │  - Python 스크립트 실행                          │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────┬───────────────────────────────────┘
                      │ subprocess.run()
                      ↓
┌─────────────────────────────────────────────────────────┐
│              Python 자동화 엔진 (src/)                  │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  main_orchestrator.py                           │   │
│  │  - 전체 프로세스 조율                            │   │
│  │  - DB 상태 업데이트                              │   │
│  └──────────────────┬──────────────────────────────┘   │
│                     │                                   │
│         ┌───────────┴────────────┐                     │
│         ↓                        ↓                     │
│  ┌─────────────────┐    ┌─────────────────┐           │
│  │ data_collector  │    │ score_calculator│           │
│  │   _parallel.py  │    │       .py       │           │
│  │                 │    │                 │           │
│  │ - 10개 카테고리 │    │ - V15.0 알고리즘│           │
│  │   병렬 수집     │    │ - 1,000점 상한  │           │
│  │ - Haiku 모델    │    │ - DB 저장       │           │
│  └─────────────────┘    └─────────────────┘           │
└─────────────────────┬───────────────────────────────────┘
                      │ Supabase Client
                      ↓
┌─────────────────────────────────────────────────────────┐
│                    Supabase Database                    │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  politician_evaluation_data                     │   │
│  │  - 평가 데이터 (카테고리별 50개)                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  evaluation_status                              │   │
│  │  - 진행 상황 추적                                │   │
│  │  - status, current_step, progress               │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  politicians                                    │   │
│  │  - 최종 점수 저장                                │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 파일 구조

```
0-3_AI_Evaluation_Engine/
├── src/
│   ├── main_orchestrator.py         # 메인 오케스트레이터
│   ├── data_collector_parallel.py   # 병렬 데이터 수집기
│   └── score_calculator.py          # 점수 계산기
│
├── instructions/                     # 카테고리별 작업지시서
│   ├── category_1_expertise.md
│   ├── category_2_leadership.md
│   ├── category_3_vision.md
│   ├── category_4_integrity.md
│   ├── category_5_ethics.md
│   ├── category_6_accountability.md
│   ├── category_7_transparency.md
│   ├── category_8_communication.md
│   ├── category_9_responsiveness.md
│   └── category_10_publicinterest.md
│
└── 설계문서_V5.0/
    ├── README.md
    ├── V13.0_작업지시서_전체_10개카테고리.md
    ├── V14.0_점수계산_알고리즘_FINAL.md
    └── 자동화_아키텍처_설계서.md (이 파일)
```

---

## 4. 실행 흐름 상세

### 4.1 STEP 0: 평가 시작

```python
# main_orchestrator.py
update_evaluation_status(
    politician_id=1,
    status="in_progress",
    step="평가 준비 중",
    progress=0
)
```

### 4.2 STEP 1: 데이터 수집 (병렬)

```python
# main_orchestrator.py가 data_collector_parallel.py 실행
subprocess.run([
    sys.executable,
    "src/data_collector_parallel.py",
    "--politician_id", "1",
    "--politician_name", "오세훈"
])

# data_collector_parallel.py
# ThreadPoolExecutor로 10개 카테고리 동시 수집
with ThreadPoolExecutor(max_workers=10) as executor:
    futures = {
        executor.submit(collect_category_data, 1, "오세훈", cat_num): cat_num
        for cat_num in range(1, 11)
    }
```

**병렬 처리 이점:**
- 순차 실행: 10 × 30초 = 5분
- 병렬 실행: 30초 (83% 시간 단축)

### 4.3 STEP 2: 점수 계산

```python
# main_orchestrator.py가 score_calculator.py 실행
subprocess.run([
    sys.executable,
    "src/score_calculator.py",
    "--politician_id", "1",
    "--politician_name", "오세훈"
])

# score_calculator.py
# V14.0 알고리즘으로 점수 계산
for cat_num in range(1, 11):
    category_score = (6.0 + avg_rating * 0.5) * 10

final_score = sum(10개 카테고리 점수)
```

### 4.4 STEP 3: 완료

```python
# main_orchestrator.py
update_evaluation_status(
    politician_id=1,
    status="completed",
    step="평가 완료",
    progress=100
)
```

---

## 5. API 설계

### 5.1 Backend API 엔드포인트

```typescript
// 1_Frontend/src/app/api/evaluate-politician/route.ts

export async function POST(request: Request) {
  const { politician_id, politician_name } = await request.json();

  // Python 스크립트 실행
  const process = spawn('python', [
    'src/main_orchestrator.py',
    '--politician_id', politician_id.toString(),
    '--politician_name', politician_name
  ], {
    cwd: '0-3_AI_Evaluation_Engine'
  });

  return NextResponse.json({
    message: 'Evaluation started',
    politician_id,
    politician_name
  });
}
```

### 5.2 Frontend 호출

```typescript
// 관리자 대시보드
async function startEvaluation(politicianId: number, politicianName: string) {
  const response = await fetch('/api/evaluate-politician', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      politician_id: politicianId,
      politician_name: politicianName
    })
  });

  // 진행 상황 실시간 조회
  const intervalId = setInterval(async () => {
    const status = await fetchEvaluationStatus(politicianId);
    updateUI(status);

    if (status.status === 'completed') {
      clearInterval(intervalId);
    }
  }, 1000);
}
```

---

## 6. 데이터베이스 스키마

### 6.1 evaluation_status 테이블

```sql
CREATE TABLE evaluation_status (
  politician_id INT PRIMARY KEY,
  status VARCHAR(20),  -- 'pending' | 'in_progress' | 'completed' | 'failed'
  current_step VARCHAR(100),
  progress INT,  -- 0~100
  updated_at TIMESTAMP
);
```

### 6.2 상태 업데이트 예시

| politician_id | status | current_step | progress | updated_at |
|---------------|--------|--------------|----------|------------|
| 1 | in_progress | 데이터 수집 중 | 50 | 2025-11-11 14:00:00 |

---

## 7. 비용 최적화

### 7.1 모델 선택: Haiku vs Sonnet 4.5

| 항목 | Sonnet 4.5 | Haiku 3.5 | 절감률 |
|------|------------|-----------|--------|
| 입력 비용 (MTok) | $3.00 | $1.00 | 67% |
| 출력 비용 (MTok) | $15.00 | $5.00 | 67% |
| 10개 카테고리 비용 | $1.50 | $0.40 | **73%** |

**선택**: Haiku 3.5 (claude-3-5-haiku-20241022)

### 7.2 병렬 처리

- 순차 실행: 5분
- 병렬 실행: 30초
- **시간 절감**: 83%

---

## 8. 실행 방법

### 8.1 단일 정치인 평가 (CLI)

```bash
cd 0-3_AI_Evaluation_Engine
python src/main_orchestrator.py --politician_id=1 --politician_name="오세훈"
```

### 8.2 Backend API 실행

```bash
# Frontend에서
cd 1_Frontend
npm run dev

# 관리자 대시보드에서 버튼 클릭
# → Backend API 호출
# → Python 스크립트 자동 실행
```

---

## 9. 모니터링 및 로깅

### 9.1 진행 상황 추적

```python
# evaluation_status 테이블에서 실시간 조회
status = supabase.table('evaluation_status')\
    .select('*')\
    .eq('politician_id', 1)\
    .execute()

print(f"진행률: {status.data[0]['progress']}%")
print(f"현재 단계: {status.data[0]['current_step']}")
```

### 9.2 로그 출력

```
============================================================
🚀 정치인 평가 시작: 오세훈 (ID: 1)
============================================================

============================================================
📊 STEP 1: 데이터 수집 시작
============================================================

  [1/10] 카테고리 1 수집 중...
  [2/10] 카테고리 2 수집 중...
  ...
  ✅ 카테고리 1 완료 (50개)
  ✅ 카테고리 2 완료 (50개)
  ...

============================================================
✅ 수집 완료: 10/10 카테고리 성공
============================================================

============================================================
🧮 STEP 2: 점수 계산 시작
============================================================

  카테고리 1: 75.0점 (평균등급: +1.5)
  카테고리 2: 72.5점 (평균등급: +1.0)
  ...

============================================================
✅ 최종 점수: 742.5점
============================================================

============================================================
✅ 정치인 평가 완료: 오세훈
============================================================
```

---

## 10. 에러 처리

### 10.1 데이터 수집 실패

```python
try:
    result = subprocess.run([...], check=True)
except subprocess.CalledProcessError as e:
    update_evaluation_status(
        politician_id=politician_id,
        status="failed",
        step="데이터 수집 실패",
        progress=10
    )
```

### 10.2 재시도 로직

- 카테고리별 수집 실패 시: 해당 카테고리만 재시도 (최대 3회)
- 전체 실패 시: 관리자에게 알림

---

## 11. 향후 개선 방향

### 11.1 Phase 2
- [ ] 실시간 WebSocket 연결
- [ ] 진행 상황 스트리밍

### 11.2 Phase 3
- [ ] 다중 정치인 동시 평가
- [ ] 우선순위 큐 시스템

### 11.3 Phase 4
- [ ] 머신러닝 기반 등급 예측
- [ ] 자동 이상치 탐지

---

**작성자**: AI 평가 엔진 개발팀
**승인자**: 시스템 관리자
**승인일**: 2025-11-11
