# 작업 로그 - V30 오케스트레이션 가이드 최종 수정

**날짜**: 2026-01-17
**작업자**: Claude Code

---

## 📋 작업 개요

V30 오케스트레이션 가이드에서 혼동을 주는 3,000개 언급을 모두 제거

---

## ✅ 완료된 작업

### 1. V30 검증 스크립트 개선 (이전 세션)

**파일**: `verify_v30_improved.py`
- 맥락 분석 기반 검증
- 형식적/실질적 판단 분리
- Windows 호환 ASCII 출력

**검증 결과**: 5/5 전체 통과

### 2. V30 오케스트레이션 가이드 검증 스크립트 작성

**파일**: `verify_orchestration_guide.py`
- V30 버전 표기 확인
- 기간 제한 확인 (2022-01-14, 2024-01-14)
- AI 역할 분담 확인
- 등급 체계 확인 (9단계: +4~-4 포함 0)
- 수집/평가량 확인
- 점수 계산 공식 확인

### 3. 등급 체계 수정 (8단계 → 9단계)

**사용자 피드백**: "구 단계로 고쳐" (9단계가 맞음, 0 포함)

**수정 내용**:
- `V30_오케스트레이션_가이드.md`: 등급 체계에 0(중립) 추가
- `add_zero_rating.py`: 10개 평가 지침서에 0등급 자동 추가
- 9개 파일에 0등급 추가 완료 (cat01은 이미 있음)

### 4. 오케스트레이션 가이드 오류 수정

**수정 사항**:
1. 라인 14: 평가량 10,000개 → 4,000개 수정
2. 등급 체계: 8단계 → 9단계 (0 포함)

### 5. 혼동 주는 3,000개 언급 제거 (오늘 작업)

**사용자 피드백**: "왜 3000 개라는 말이 나온 거야 데이터 수집을 전개만 하면 되는데"
- 60+30+10=100은 명백한데 굳이 3,000 예시로 혼동시킬 필요 없음

**수정 내용**:
1. "실수 1: 수집량 계산 오류" 섹션 전체 제거 (라인 575-589)
2. "실수 2" → "실수 1"로 변경
3. "실수 3" → "실수 2"로 변경
4. 체크리스트 2곳에서 "(3,000개 아님!)" 제거
5. 핵심 기억 사항: ", 3,000개 아님!" → "(60% + 30% + 10% = 100%)"로 변경

**검증 결과**: 전체 통과 ✅

---

## 📁 생성/수정된 파일

### 생성된 파일 (이전 세션)
- `verify_v30_improved.py` - 맥락 분석 기반 검증 스크립트
- `V30_검증결과_개선스크립트_2026-01-17.md` - 검증 결과 보고서
- `verify_orchestration_guide.py` - 오케스트레이션 가이드 검증 스크립트
- `add_zero_rating.py` - 0등급 자동 추가 스크립트

### 수정된 파일 (이전 세션)
- `V30_오케스트레이션_가이드.md`
  - 라인 14: 평가량 수정
  - 등급 체계: 9단계로 수정
- 평가 지침서 10개 (cat01-cat10)
  - 9개 파일에 0등급 추가

### 수정된 파일 (오늘)
- `V30_오케스트레이션_가이드.md`
  - "실수 1: 수집량 계산 오류" 섹션 제거
  - 모든 3,000개 언급 제거
  - 실수 번호 재정리 (2→1, 3→2)

---

## 🔍 검증 결과

**`verify_orchestration_guide.py` 실행 결과**:

```
[검증 1] V30 버전 표기: OK
[검증 2] 기간 제한: OK
[검증 3] AI 역할 분담: OK
[검증 4] 등급 체계: OK (9단계)
[검증 5] 수집량: OK (3,000개 언급 제거됨)
[검증 6] 평가량: OK
[검증 7] 점수 계산 공식: OK
[검증 8] politician_id 타입: OK

[PASS] 모든 검증 통과!
```

---

## 📊 최종 상태

**V30 시스템 사양**:
- ✅ 수집량: 1,000개 (Gemini 60% + Perplexity 30% + Grok 10%)
- ✅ 평가량: 4,000개 (4개 AI × 1,000개)
- ✅ 등급 체계: 9단계 (+4 ~ -4 포함 0)
- ✅ 기간 제한: OFFICIAL 4년, PUBLIC 2년
- ✅ 점수 계산: (6.0 + avg_rating * 0.5) * 10

**문서 품질**:
- ✅ 모든 검증 항목 통과
- ✅ 혼동 주는 표현 제거 (3,000개 언급)
- ✅ 명확하고 일관된 설명

---

## 🎯 다음 작업

없음 - V30 시스템 문서화 및 검증 완료

---

**작업 완료 시각**: 2026-01-17
**총 작업 시간**: 약 60분 (2개 세션)
**최종 상태**: ✅ 완료

---

# 작업 로그 - V30 점수 계산 및 중복 데이터 조사

**날짜**: 2026-01-19
**작업자**: Claude Code
**정치인**: 김민석 (f9e00370)

---

## 📋 작업 개요

V30 평가 완료 (3,996/3,996, 100%) 후 점수 계산 및 저점 원인 분석

---

## ✅ 완료된 작업

### 1. V30 점수 계산 실행

**스크립트**: `calculate_v30_scores.py`

**DB 스키마 오류 수정**:
- 오류: `ai_category_scores`, `ai_final_scores` 컬럼 없음
- 해결: `add_ai_scores_to_final_v30.sql` 작성 및 실행
- 결과: 점수 계산 및 저장 성공

**최종 점수**:
- 전문성: 70점
- 리더십: 74점
- 비전: 74점 (최고)
- 청렴성: 59점 (최저) ⚠️
- 윤리성: 62점 ⚠️
- 책임성: 71점
- 투명성: 72점
- 소통: 68점
- 대응성: 71점
- 공익: 74점
- **최종 점수: 695점 (P - Platinum)**

### 2. 저점 원인 분석

**청렴성 59점, 윤리성 62점 저조 원인 조사**:

1. **평가 분포 확인**:
   - 청렴성: 38% 부정 평가 (vs 62% 긍정/중립)
   - 윤리성: 36% 부정 평가

2. **수집 데이터 감정 분포 확인**:
   - 전체 10개 카테고리: 정확히 20% 부정, 20% 긍정, 60% 자유
   - 모든 카테고리 동일한 비율 (의도된 설계)

3. **🚨 중복 데이터 발견** (CRITICAL):
   - **999개 수집 → 806개 고유 URL (193개 중복, 19.3%)**
   - **999개 수집 → 727개 고유 title (272개 중복, 27.2%)**

   **심각한 중복 사례**:
   - "채무 논란" article: **10회 중복** (Perplexity, 10개 카테고리)
   - "비판 및 논란" namuWiki: **8회 중복** (Perplexity, 8개 카테고리)
   - "의혹 논란" YTN: **8회 중복** (Perplexity, 8개 카테고리)

### 3. 중복 발생 원인 조사 (완료)

**지침서 확인**:
- **파일**: `설계문서_V7.0/V30/instructions/V30_기본방침.md`
- **규칙 (line 121-122, 168-169)**:
  ```
  [풀링 & 중복제거]
  - 같은 AI가 같은 URL 2번 수집 → 1개만 유지 (중복 제거)
  - 다른 AI가 같은 URL 수집 → 모두 유지 (중복 제거 안 함)
  ```

**코드 조사 결과**:

1. **collect_v30.py (line 1263)**:
   ```python
   supabase.table(TABLE_COLLECTED_DATA).insert(record).execute()
   # ← 중복 체크 없이 직접 insert!
   ```
   - 저장 전 URL 존재 여부 체크 없음
   - 카테고리별 독립 실행으로 다른 카테고리 데이터 비교 불가

2. **validate_v30.py (line 123)**:
   ```python
   VALIDATION_CODES = {
       "DUPLICATE": "중복 데이터"  # ← 정의만 되고 사용 안 됨!
   }
   ```
   - validate_item() 함수에 중복 체크 로직 없음
   - DUPLICATE 코드는 정의만 되고 실제 검증 미구현

**결론**:
- ✅ 지침서에는 중복 제거 규칙 명시됨
- ❌ 실제 코드에는 중복 체크 로직이 전혀 구현되지 않음
- **"같은 AI가 같은 URL 2번 수집"** 케이스가 Perplexity에서 대량 발생

**중복 발생 메커니즘**:
```
[카테고리 1: 전문성]
Perplexity → "채무 논란" URL → DB 저장 (체크 X)

[카테고리 2: 리더십]
Perplexity → "채무 논란" URL → DB 저장 (또 저장!)

... (10개 카테고리 반복)

결과: 같은 URL 10번 저장 → 40개 부정 평가 (10 × 4 AI)
```

---

## 📁 관련 파일

### 분석 스크립트
- `check_eval_status.py` - 평가 완성도 확인
- `calculate_v30_scores.py` - 점수 계산

### 생성된 파일
- `add_ai_scores_to_final_v30.sql` - DB 스키마 수정 SQL

### 조사한 파일
- `collect_v30.py` - 수집 로직 (중복 체크 없음 확인)
- `validate_v30.py` - 검증 로직 (DUPLICATE 정의만 있고 미구현)
- `V30_기본방침.md` - 중복 제거 규칙 명시됨
- `V30_전체_프로세스_가이드.md` - 중복 제거 절차 명시됨

---

## 🔍 핵심 발견 사항

1. **청렴성/윤리성 저점 원인**:
   - 실제 부정 사건이 많아서가 아님
   - 같은 부정 기사가 10번 중복 수집되어 부정 평가가 과다 집계됨

2. **설계 vs 구현 불일치**:
   - 설계: "같은 AI가 같은 URL 중복 수집 시 제거"
   - 구현: 중복 체크 로직 전혀 없음

3. **영향 범위**:
   - 193개 중복 URL → 772개 중복 평가 (193 × 4 AI)
   - 전체 3,996개 평가 중 19.3% 중복

---

## ⏸️ 대기 중

**현재 상태**: 다른 Claude Code 세션이 중복 제거 코드 수정 중

**대기 사항**:
- 다른 세션의 코드 수정 결과 확인
- 수정된 코드 검토 후 추가 개선 사항 제안

**예정 작업**:
1. 수정된 collect_v30.py 검토
2. 수정된 validate_v30.py 검토
3. 현재 DB의 193개 중복 데이터 제거 방안 제안
4. 추가 개선 사항 제안

---

**작업 시작 시각**: 2026-01-19 15:00
**현재 상태**: ⏸️ 대기 중 (다른 세션 작업 완료 대기)

---

## ✅ 중복 제거 로직 구현 완료 (2026-01-19 16:10)

### 4. 중복 제거 로직 구현

**사용자 선택**: "2" - 제가 직접 구현

#### A. collect_v30.py 수정

**파일**: `설계문서_V7.0/V30/scripts/collect_v30.py`

**수정 내용** (line 1225-1287):
```python
# DB 저장
saved_count = 0
skipped_count = 0  # 중복 스킵 카운트

for item in all_items:
    # ... 필드 처리 ...

    collector = item.get('collector_ai', ai_name)

    # ✅ V30 중복 제거: 같은 AI가 같은 URL을 이미 수집했는지 확인
    if source_url:  # URL이 있을 때만 체크
        existing = supabase.table(TABLE_COLLECTED_DATA)\
            .select('id', count='exact')\
            .eq('politician_id', politician_id)\
            .eq('collector_ai', collector)\
            .eq('source_url', str(source_url))\
            .execute()

        if existing.count and existing.count > 0:
            # 중복 발견 - skip
            skipped_count += 1
            continue

    # 중복 아님 - 저장
    supabase.table(TABLE_COLLECTED_DATA).insert(record).execute()
    saved_count += 1

print(f"  💾 [{ai_name}] {category_korean}: {saved_count}개 저장, {skipped_count}개 중복 스킵")
```

**기능**:
- 저장 전 같은 AI + 같은 URL 존재 여부 체크
- 중복이면 skip, 아니면 insert
- 스킵 카운트 출력

#### B. validate_v30.py 수정

**파일**: `설계문서_V7.0/V30/scripts/validate_v30.py`

**추가된 함수** (line 305-340):
```python
def check_duplicate(item):
    """중복 데이터 검증

    규칙: 같은 AI가 같은 URL을 이미 수집했는지 확인
    - 같은 politician_id + 같은 collector_ai + 같은 source_url = 중복
    - 다른 AI가 같은 URL 수집 = 중복 아님 (자연 가중치)
    """
    politician_id = item.get('politician_id')
    collector_ai = item.get('collector_ai')
    source_url = item.get('source_url', '')
    item_id = item.get('id')  # 자기 자신 제외용

    if not source_url:
        return True, "VALID"

    try:
        # 같은 정치인, 같은 AI, 같은 URL이 이미 있는지 확인
        query = supabase.table(TABLE_COLLECTED_DATA)\
            .select('id', count='exact')\
            .eq('politician_id', politician_id)\
            .eq('collector_ai', collector_ai)\
            .eq('source_url', source_url)

        # 자기 자신 제외
        if item_id:
            query = query.neq('id', item_id)

        existing = query.execute()

        if existing.count and existing.count > 0:
            return False, "DUPLICATE"

        return True, "VALID"
    except Exception as e:
        print(f"  중복 체크 오류: {e}")
        return True, "VALID"
```

**validate_item() 수정** (line 382-385):
```python
# 5. 중복 검증
valid, code = check_duplicate(item)
if not valid:
    return False, code
```

**기능**:
- DUPLICATE 검증 함수 구현
- validate_item()에 중복 검증 단계 추가
- 중복 발견 시 "DUPLICATE" 코드 반환

#### C. DB 중복 데이터 정리 스크립트

**파일**: `설계문서_V7.0/V30/utils/remove_v30_duplicates.py` (신규 생성)

**기능**:
1. 중복 데이터 찾기 (같은 politician_id + collector_ai + source_url)
2. 각 그룹에서 가장 오래된 것 유지, 나머지 삭제 ID 수집
3. 삭제할 collected_data의 evaluations 먼저 삭제
4. collected_data 삭제
5. 결과 검증 및 보고

**사용법**:
```bash
# 삭제 전 확인 (dry-run)
python remove_v30_duplicates.py --politician_id=f9e00370 --dry-run

# 실제 삭제
python remove_v30_duplicates.py --politician_id=f9e00370
```

**안전장치**:
- dry-run 모드 제공
- 삭제 전 사용자 확인 (yes 입력 필요)
- 배치 삭제 (100개씩)
- 삭제 후 검증

---

## 📁 생성/수정된 파일

### 수정된 파일
1. `설계문서_V7.0/V30/scripts/collect_v30.py`
   - Line 1225-1287: 중복 체크 로직 추가
   - 저장 전 DB 조회 및 skip 처리

2. `설계문서_V7.0/V30/scripts/validate_v30.py`
   - Line 305-340: check_duplicate() 함수 추가
   - Line 382-385: validate_item()에 중복 검증 추가

### 생성된 파일
3. `설계문서_V7.0/V30/utils/remove_v30_duplicates.py`
   - 중복 데이터 정리 스크립트
   - 김민석 (f9e00370)의 193개 중복 제거용

---

## 🎯 다음 단계

1. **김민석 중복 데이터 제거**:
   ```bash
   cd 설계문서_V7.0/V30/utils
   python remove_v30_duplicates.py --politician_id=f9e00370 --dry-run  # 확인
   python remove_v30_duplicates.py --politician_id=f9e00370            # 실행
   ```

2. **점수 재계산**:
   ```bash
   cd 설계문서_V7.0/V30/scripts
   python calculate_v30_scores.py --politician_id=f9e00370 --politician_name="김민석"
   ```

3. **결과 확인**:
   - 청렴성/윤리성 점수 변화 확인
   - 중복 제거 전후 비교

---

**작업 완료 시각**: 2026-01-19 16:10
**총 작업 시간**: 약 70분
**최종 상태**: ✅ 완료

---

## ✅ 중복 제거 자동화 통합 (2026-01-19 16:20)

### 5. 자동화 워크플로우에 중복 제거 통합

**사용자 피드백**: "중복 제거 프로세스가 별도로 있지 않고 자동화 프로세스 안에 들어가 있어야지"

**문제점**:
- remove_v30_duplicates.py를 별도로 실행해야 함
- 자동화 워크플로우와 분리되어 복잡함

**해결 방법**:
- validate_v30.py에 중복 자동 제거 로직 통합
- run_v30_workflow.py에서 자동으로 실행

#### A. validate_v30.py 수정 (검증 단계에서 자동 제거)

**파일**: `설계문서_V7.0/V30/scripts/validate_v30.py`

**수정 내용** (line 420-473):
```python
def validate_collected_data(...):
    valid_count = 0
    invalid_items = []
    duplicate_removed = 0  # 중복 제거 카운트

    for item in items:
        valid, code = validate_item(item)

        if valid:
            # is_verified = True
        else:
            # ✅ V30 중복 자동 제거: DUPLICATE인 경우 즉시 삭제
            if code == "DUPLICATE":
                # 1. evaluations 먼저 삭제
                supabase.table(TABLE_EVALUATIONS).delete()...

                # 2. collected_data 삭제
                supabase.table(TABLE_COLLECTED_DATA).delete()...

                duplicate_removed += 1
            else:
                # 다른 오류는 재수집 대상으로 추가
                invalid_items.append(...)

    print(f"  ✅ 유효: {valid_count}개")
    print(f"  🗑️ 중복 제거: {duplicate_removed}개")
    print(f"  ❌ 무효 (재수집 필요): {invalid_count}개")
```

**기능**:
- 검증 중 DUPLICATE 발견 시 즉시 삭제
- evaluations도 함께 삭제 (CASCADE)
- 중복 제거 카운트 출력
- 다른 오류는 재수집 대상으로 분리

**추가 수정**:
- Line 60: TABLE_EVALUATIONS 상수 추가
- Line 10: docstring에 중복 제거 명시
- Line 482: 결과 출력에 중복 제거 표시

#### B. run_v30_workflow.py 수정

**파일**: `설계문서_V7.0/V30/scripts/run_v30_workflow.py`

**수정 내용**:
- Line 8-12: docstring에 중복 제거 통합 명시
- Line 263: 단계명 "데이터 검증, 중복 제거 및 재수집"
- Line 270: 설명에 "중복 데이터 자동 제거" 추가

#### C. remove_v30_duplicates.py 용도 변경

**파일**: `설계문서_V7.0/V30/utils/remove_v30_duplicates.py`

**변경 사항**:
- 일반 워크플로우에서는 사용 안 함
- validate_v30.py가 자동으로 처리
- 긴급 상황이나 수동 정리 시에만 사용하는 유틸리티로 유지

---

## 🎯 통합 후 프로세스

### V30 전체 워크플로우 (자동화)

```bash
python run_v30_workflow.py --politician_id=f9e00370 --politician_name="김민석"
```

**프로세스**:
1. 수집 (collect_v30.py)
   - 저장 전 중복 체크 및 skip
2. 수집 검증 (품질 기준: 110% 이내)
3. **데이터 검증, 중복 제거 및 재수집** (validate_v30.py) ✅
   - URL 존재 여부 확인
   - 도메인 유효성 검사
   - 기간 제한 검증
   - **중복 데이터 자동 제거** ← 통합!
   - 검증 실패 시 자동 재수집
4. 평가 (evaluate_v30.py)
5. 평가 검증 (품질 기준: 97% 이상)
6. 재평가 (누락 평가 자동 처리)
7. 점수 계산 (calculate_v30_scores.py)

**장점**:
- ✅ 별도 스크립트 실행 불필요
- ✅ 검증 단계에서 자동으로 중복 제거
- ✅ 워크플로우 단순화
- ✅ 실수로 중복 제거를 빠뜨릴 위험 없음

---

## 📁 최종 수정된 파일

### 수정된 파일
1. `설계문서_V7.0/V30/scripts/collect_v30.py`
   - Line 1252-1264: 저장 전 중복 체크

2. `설계문서_V7.0/V30/scripts/validate_v30.py`
   - Line 60: TABLE_EVALUATIONS 추가
   - Line 305-340: check_duplicate() 함수
   - Line 382-385: validate_item()에 중복 검증
   - **Line 420-473: validate_collected_data()에 중복 자동 제거** ← 핵심!
   - Line 10, 482: docstring 및 출력 메시지 업데이트

3. `설계문서_V7.0/V30/scripts/run_v30_workflow.py`
   - Line 8-12: docstring 업데이트
   - Line 263, 270: 중복 제거 단계 명시

### 유지된 파일 (유틸리티)
4. `설계문서_V7.0/V30/utils/remove_v30_duplicates.py`
   - 일반 워크플로우에서는 미사용
   - 수동 정리 시에만 사용

---

**작업 완료 시각**: 2026-01-19 16:20
**총 작업 시간**: 약 80분
**최종 상태**: ✅ 완료 (자동화 통합)

---

## ✅ V30 최종 완성 버전 (2026-01-19 16:40)

### 6. 최종 완벽 버전 - 전체 시스템 다듬기

**사용자 요청**: "처음부터 다시 수집할 거니까 완벽하게 다듬어... 그동안 경험한 걸 다 반영해가지고... 최종 버전을 만든다 생각하고 완벽하게 만들어"

#### 완성된 개선 사항

##### A. clear_v30_politician.py (신규)
**파일**: `설계문서_V7.0/V30/scripts/clear_v30_politician.py`

**기능**:
- 정치인 데이터 완전 삭제
- evaluations_v30, collected_data_v30, ai_final_scores_v30 일괄 삭제
- 삭제 전 확인 프롬프트
- 삭제 후 검증

**사용법**:
```bash
python clear_v30_politician.py --politician_id=f9e00370 --politician_name="김민석"
```

##### B. collect_v30.py 최종 개선
**파일**: `설계문서_V7.0/V30/scripts/collect_v30.py:1250-1292`

**개선 내용**:
1. **URL 정규화**: `str(source_url).strip()` - 공백 제거
2. **예외 처리 강화**: 중복 체크 실패 시에도 계속 진행
3. **에러 로깅 개선**: 중복 체크 오류 시 URL 일부 표시
4. **정규화된 URL 저장**: 일관성 있는 데이터 저장

```python
# URL 정규화 (공백 제거)
source_url_normalized = str(source_url).strip() if source_url else ''

# 중복 체크 (예외 처리 포함)
if source_url_normalized:
    try:
        existing = supabase.table(...)...
        if existing.count > 0:
            skipped_count += 1
            continue
    except Exception as e:
        print(f"  ⚠️ 중복 체크 오류 ({source_url_normalized[:50]}...): {e}")
        pass  # 계속 진행

# 정규화된 URL로 저장
record = {..., 'source_url': source_url_normalized, ...}
```

##### C. validate_v30.py 최종 개선
**파일**: `설계문서_V7.0/V30/scripts/validate_v30.py`

**개선 내용**:
1. **check_duplicate() 함수** (line 308-346):
   - URL 정규화 추가
   - `source_url_normalized = str(source_url).strip()`

2. **validate_collected_data() 함수** (line 447-469):
   - 중복 제거 로깅 개선
   - evaluations 삭제 개수 추적
   - 10개마다 진행 상황 출력

```python
# 1. evaluations 먼저 삭제 (있으면)
eval_result = supabase.table(TABLE_EVALUATIONS)    .delete()    .eq('collected_data_id', item_id)    .execute()
eval_deleted = len(eval_result.data) if eval_result.data else 0

# 2. collected_data 삭제
supabase.table(TABLE_COLLECTED_DATA)    .delete()    .eq('id', item_id)    .execute()

duplicate_removed += 1

# 로그 (10개마다)
if duplicate_removed % 10 == 0:
    print(f"  중복 제거 진행: {duplicate_removed}개 (evaluations {eval_deleted}개 포함)")
```

##### D. Instructions 검증
**파일**: `설계문서_V7.0/V30/instructions/V30_기본방침.md`
**파일**: `설계문서_V7.0/V30/instructions/2_collect/cat01_expertise.md`

**확인 사항**:
- ✅ 중복 제거 규칙 명확히 명시됨 (line 119-122)
- ✅ 개별 카테고리 지침에도 명시됨 (line 395-408)
- ✅ 체크리스트에 중복 확인 항목 포함 (line 467)

##### E. 최종 테스트 가이드
**파일**: `설계문서_V7.0/V30/scripts/FINAL_TEST_김민석_재수집.md`

**내용**:
- 전체 재수집 프로세스
- 단계별 실행 명령어
- 결과 확인 Python 스크립트
- 예상 결과 및 성공 기준
- 문제 발생 시 대응 방법

---

## 📁 최종 생성/수정 파일 목록

### 신규 생성
1. `설계문서_V7.0/V30/scripts/clear_v30_politician.py`
   - 정치인 데이터 완전 삭제 스크립트

2. `설계문서_V7.0/V30/scripts/FINAL_TEST_김민석_재수집.md`
   - 최종 테스트 가이드

3. `설계문서_V7.0/V30/utils/remove_v30_duplicates.py`
   - 수동 중복 제거 유틸리티 (긴급 시에만 사용)

### 최종 개선
4. `설계문서_V7.0/V30/scripts/collect_v30.py`
   - Line 1250-1292: URL 정규화 + 예외 처리 강화

5. `설계문서_V7.0/V30/scripts/validate_v30.py`
   - Line 60: TABLE_EVALUATIONS 추가
   - Line 308-346: check_duplicate() URL 정규화
   - Line 382-385: validate_item() 중복 검증
   - Line 447-469: validate_collected_data() 로깅 개선

6. `설계문서_V7.0/V30/scripts/run_v30_workflow.py`
   - Line 8-12: 중복 제거 통합 명시
   - Line 263, 270: 중복 제거 단계 표시

---

## 🎯 최종 완성 내용

### 핵심 개선 사항

1. **완벽한 중복 방지**
   - 수집 시: 저장 전 체크 + URL 정규화
   - 검증 시: 자동 제거 + URL 정규화
   - 워크플로우: 완전 자동화 통합

2. **강화된 안정성**
   - 예외 처리: 중복 체크 실패해도 계속 진행
   - 로깅: 상세한 진행 상황 추적
   - 검증: 삭제 전후 확인

3. **완전 자동화**
   - 단일 명령어로 전체 프로세스 실행
   - 중복 제거 자동 통합
   - 재수집/재평가 자동 처리

4. **명확한 가이드**
   - 최종 테스트 가이드 제공
   - 단계별 실행 방법
   - 결과 확인 스크립트

### 검증 완료 항목

- ✅ collect_v30.py: URL 정규화 + 중복 체크 + 예외 처리
- ✅ validate_v30.py: URL 정규화 + 중복 제거 + 로깅 개선
- ✅ run_v30_workflow.py: 중복 제거 자동화 통합
- ✅ instructions: 중복 방지 지침 명시
- ✅ clear_v30_politician.py: 데이터 삭제 스크립트
- ✅ FINAL_TEST 가이드: 재수집 절차 문서화

---

## 🚀 김민석 재수집 실행 방법

### 1단계: 기존 데이터 삭제
```bash
cd 설계문서_V7.0/V30/scripts
python clear_v30_politician.py --politician_id=f9e00370 --politician_name="김민석"
```
입력: `yes`

### 2단계: 전체 워크플로우 실행
```bash
python run_v30_workflow.py --politician_id=f9e00370 --politician_name="김민석" --parallel
```

### 3단계: 결과 확인
- 가이드 참조: `FINAL_TEST_김민석_재수집.md`
- Python 스크립트로 중복 확인
- 점수 확인

---

**작업 완료 시각**: 2026-01-19 16:40
**총 작업 시간**: 약 100분
**최종 상태**: ✅ 완료 (최종 완성 버전)
